const a = () => "hello" + "world"
const b = (x) => "hello" + x
const c = (x) => "hello" + x + "again"
const d = (x) => "hello" + x + "again" + x
const e = (x,y) => "hello" + x + y
// const f = (x) => x + x TODO: figure out the types for these