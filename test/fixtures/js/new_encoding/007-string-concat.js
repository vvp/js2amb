const a = () => "hello" + "world"
const b = (x) => "hello" + x
const c = (x) => "hello" + x + "again"
const d = (x) => "hello" + x + "again" + x
const e = (x,y) => "hello" + x + y
const f = (x,y) => x + y
const g = (x) => x + x
const h = (x,y,z) => x + y + (y + x)
