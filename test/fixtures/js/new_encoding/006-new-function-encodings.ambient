a[
  write_ (init).(
    {init}[]|
    write_ (x).read_ (x)
  )
]|
b[
  write_ (init).(
    {init}[]|
    write_ (fn, x).write (fn, x).read_ (fn, fn_r0).(
      fn_r0[]|
      read_ (fn_r0)
    )
  )
]|
c[
  write_ (init).(
    :init|
    write_ (fn, x).write (fn, x).read_ (fn, fn_r0).(
      string[plus[l[fn_r0[]]|r[string[hello[]]]|
      read_ (string)
    )
  )
]